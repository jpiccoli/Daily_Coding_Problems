#pragma once

#include <algorithm>
#include <iostream>
#include <string>
#include <vector>
#include <algorithm>

std::pair<long, long> number_456();
std::vector<int> number_489(std::vector<int> arr);
bool number_491(int value);

